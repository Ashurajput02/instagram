import 'package:flutter/cupertino.dart';

class postitem extends StatelessWidget {  @override
  Widget build(BuildContext context) {
    // TODO: implement build
const postitem({super.key});
    throw UnimplementedError();
  }
}
